
info:
  title: STD23036 - TD1
  version: 1.0.0

servers:
  - url: https://localhost.STD23036.com

paths:
  /customers:
    get:
      responses:
        '200':
          description: Succès
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Customer"

components:
  schemas:
    Customer:
      type: object
      properties:
        name:
          type: string
        age:
          type: integer